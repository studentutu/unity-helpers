name: Validate Formatting

on:
  push:
    branches:
      - main
    paths:
      - "**.md"
      - "**.json"
      - "**.yml"
      - "**.yaml"
      - "**.js"
      - "**.cs"
      - "scripts/validate-formatting.sh"
      - "scripts/tests/test-final-newline.sh"
      - ".github/workflows/validate-formatting.yml"
  pull_request:
    paths:
      - "**.md"
      - "**.json"
      - "**.yml"
      - "**.yaml"
      - "**.js"
      - "**.cs"
      - "scripts/validate-formatting.sh"
      - "scripts/tests/test-final-newline.sh"
      - ".github/workflows/validate-formatting.yml"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  validate-formatting:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Setup Node.js (cached)
        if: ${{ hashFiles('package-lock.json') != '' }}
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: package-lock.json

      - name: Setup Node.js (no cache)
        if: ${{ hashFiles('package-lock.json') == '' }}
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: "22"

      - name: Install dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm i --no-audit --no-fund
          fi

      - name: Test final newline
        run: bash scripts/tests/test-final-newline.sh

      - name: Test stdin passthrough contract
        # The runner this job's formatting checks go through used to drain stdin, which failed with
        # EAGAIN on a non-blocking pipe and reported a phantom lint failure. Runs here because this
        # is the job that installs the Node tooling the contract exercises.
        run: node scripts/tests/test-read-stdin-sync.js

      - name: Validate formatting
        run: bash scripts/validate-formatting.sh
